<main>
	
	<%= form_for @image, :url => {:controller => 'images', :action => 'create'}, :html => { :multipart => true}  do |f|%>
		<%= f.label :avatar %>
		<%= f.file_field :avatar %>
		<br>
		<%= f.label :name %>
		<%= f.text_field :name %>
		<br>
		<%= f.submit "Upload Image" %>
	<% end %>
</main>